import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SecuredPaymentsComponent } from './secured-payments.component';

describe('SecuredPaymentsComponent', () => {
  let component: SecuredPaymentsComponent;
  let fixture: ComponentFixture<SecuredPaymentsComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [SecuredPaymentsComponent]
    });
    fixture = TestBed.createComponent(SecuredPaymentsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
