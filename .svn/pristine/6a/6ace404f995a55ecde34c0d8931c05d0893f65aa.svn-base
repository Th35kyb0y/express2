import { ComponentFixture, TestBed } from '@angular/core/testing';

import { VolvoBusComponent } from './volvo-bus.component';

describe('VolvoBusComponent', () => {
  let component: VolvoBusComponent;
  let fixture: ComponentFixture<VolvoBusComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [VolvoBusComponent]
    });
    fixture = TestBed.createComponent(VolvoBusComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
