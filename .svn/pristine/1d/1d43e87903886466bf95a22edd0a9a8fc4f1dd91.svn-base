import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateProspectPopupComponent } from './create-prospect-popup.component';

describe('CreateProspectPopupComponent', () => {
  let component: CreateProspectPopupComponent;
  let fixture: ComponentFixture<CreateProspectPopupComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [CreateProspectPopupComponent]
    });
    fixture = TestBed.createComponent(CreateProspectPopupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
