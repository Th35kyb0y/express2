import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DuctDetailsModalComponent } from './duct-details-modal.component';

describe('DuctDetailsModalComponent', () => {
  let component: DuctDetailsModalComponent;
  let fixture: ComponentFixture<DuctDetailsModalComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DuctDetailsModalComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DuctDetailsModalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
